import 'package:flutter/material.dart';

class ContactPhoto extends StatelessWidget {
  const ContactPhoto();
  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Image.asset("contact.png"),
    );
  }
}